on:
  push:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - run: |
          <warning descr="Command 'set-output' is deprecated and will be removed in the future.See GitHub documentation for more details.">echo "::set-output name=MY_VAR::MY_VALUE"</warning>
